services:

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      NUXT_API_BASE: http://api:8000
    networks:
      - backend_network
    restart: unless-stopped
    depends_on:
      - api

  api:
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./zernyshko:/src/zernyshko
      - ./uploads:/src/uploads
      - ./alembic:/src/alembic
    networks:
      - backend_network
    env_file:
      - ./.env
    command: "poetry run uvicorn --factory zernyshko.api.main:create_app --host 0.0.0.0 --port 8000"
    restart: always
    depends_on:
      postgres:
        condition: service_healthy  
      redis:
        condition: service_healthy

  postgres:
    image: postgres:18.1
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: database
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql 
    networks:
      - backend_network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "user", "-d", "database"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis: 
    image: redis:latest
    container_name: redis
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - backend_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
  redis_data:
  minio_data:

networks:
  backend_network:
    name: backend_network
    driver: bridge